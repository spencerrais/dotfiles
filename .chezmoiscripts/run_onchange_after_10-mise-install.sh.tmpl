#!/usr/bin/env bash
set -euo pipefail

# Re-run when mise config changes:
# {{ include "dot_config/mise/config.toml.tmpl" | sha256sum }}

if ! command -v mise >/dev/null 2>&1; then
  echo "mise is required but not installed."
  exit 1
fi

mise install
